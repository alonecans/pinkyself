## Pinky Self

```bash
> termux-setup-storage
> apt update && apt upgrade
> pkg install nodejs
> pkg install git
> pkg install bash
> git clone https://github.com/alonecans/pinkyself.git
> cd pinkyself
> bash install.sh
> npm install
> node nuy
```